export const apiBaseUrl: String = 'http://localhost:8080/api';
export const topicsBaseUrl = `${apiBaseUrl}/topics`;
export const questionBaseUrl = `${apiBaseUrl}/questions`;
export const commentBaseUrl = `${apiBaseUrl}/comments`;
